import * as diagram from "../diagram";
export declare class Generator {
    private readonly rootGraph;
    constructor(dia: diagram.Diagram);
    generatePng(path: string): Promise<string>;
    generateDot(path: string): string;
    private breakLineEveryMaxChars;
    private wordsToLabel;
    private diagramGraph;
    private addToGraph;
    private addEdges;
    private findFirstArbitraryClusterDirectLeaf;
    private dotToPng;
    private static graphvizBinary;
}
